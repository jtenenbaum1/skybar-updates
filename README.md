# Skybar updates

Sparkle appcast and release archives for [Skybar](https://github.com/jtenenbaum1/Skybar).

The app fetches `appcast.xml` over HTTPS and verifies it with the EdDSA public key in `public-key.txt`, which is also baked into every release build.
